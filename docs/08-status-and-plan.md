# 08 — สถานะจริง + แผนที่เหลือ

**อัปเดต:** 2026-07-30 (rev.3) · เอกสารนี้ตอบคำถามเดียว: **"เหลืออะไรอีก"**
ตัวเลขทั้งหมดนับจาก `backlog.md` และไฟล์จริง ไม่ใช่ประมาณการ

> **rev.3 แก้อะไร:**
> · spec พร้อมเพิ่มจาก 27 → **31** (เขียน SPEC-027 · 028 · 029 · 062 เสร็จ)
> · **SPEC-027 กลับด้าน dependency** — ไม่ขึ้นกับ SPEC-025 แล้ว จึง**หลุดจากเงา D9/D10**
> · SPEC-063 เข้ารอบ review ที่ 2 → `CHANGES_REQUIRED` · **เจอปัญหาที่ gate ซึ่งใหญ่กว่าตัวโค้ด**
> · เหลือ spec ที่ Claude เขียนได้แต่ยังไม่ได้เขียน: **0 ฉบับ** — ที่เหลือติด D9/D10 หรือติดของจริง

---

## 1. ตอนนี้อยู่ตรงไหน

| | จำนวน |
|---|---|
| ticket ทั้งหมด | **66** |
| ✅ เสร็จจริง (implement + review ผ่าน) | **1** (SPEC-003 schema) |
| 🔨 กำลังทำ | **2** (SPEC-001 รอบ 1.5/1.6 · SPEC-063 รอบ 2) |
| 📄 มี spec พร้อมให้ Codex ทำ | **31** |
| 🔴 มี spec ไม่ได้เพราะรอเจ้าของ | **3** (SPEC-024 · 025 · 026 — D9/D10) |
| ⬜ ยังไม่มี spec (Phase 3 ขึ้นไป + 030/030b) | **31** |

**งานที่เสร็จจริง ~1.5% · งานที่ *พร้อมทำทันที* ~47%**

Codex มีงานในมือแล้ว **~570 test ข้าม 31 ticket** — ไม่มีทางว่างในระยะสั้น

### 🔴 เรื่องด่วนที่สุดตอนนี้ไม่ใช่ ticket ไหนเลย — คือ **gate เชื่อไม่ได้**

[review รอบ 2](reviews/SPEC-063-02.md) §T พบว่า `run-mql5-tests.ps1` + `compile-gate.ps1`
**รายงาน `PASSED` ได้ทั้งที่ไม่มีการคอมไพล์เกิดขึ้น**:

| | |
|---|---|
| `.ex5` และ compile log **ไม่ถูกลบก่อน compile** | compile พัง → binary เก่ายังอยู่ → tester รันของเก่า |
| บรรทัด `Result: N errors` **ถูกพิมพ์แต่ไม่เคยตรวจ** | 5 errors ก็เดินต่อ |
| `git_sha` **ฉีดผ่าน `.set` ไม่ได้ compile ติดมา** | ตาข่ายที่คิดว่ามี **ไม่รองรับอะไรเลย** |
| `$RequiredSuiteNames[$name]` ไม่มี `ContainsKey` guard | suite ที่ลืมใส่ = ผ่านฟรีเงียบๆ (**ถอยหลังจากเดิม**) |

**ผลกระทบ:** คำกล่าวอ้าง "test ผ่าน" ทุกข้อของรอบนี้ยังพิสูจน์ไม่ได้
· และ attestation ของ SPEC-065/009 จะไม่มีความหมายจนกว่าจะแก้ + commit

### หลักฐานสถานะปัจจุบัน (working tree, branch `feat/SPEC-001-mt5-executor`)

| ไฟล์ | แปลว่า |
|------|--------|
| `tests/chaos/live_mt5_harness.py` (ใหม่) | รอบ 1.5 — chaos harness · [review](reviews/SPEC-016-chaos-01.md) `CHANGES_REQUIRED` (C1–C13) |
| `mt5-ea/Include/Farm/BrokerTime.mqh` + `tests/mql5/TestBrokerTime.mq5` (ใหม่) | รอบ 2 — SPEC-063 · **แก้มา 2 รอบแล้ว** · [review 2](reviews/SPEC-063-02.md) `CHANGES_REQUIRED` |
| `tools/run-mql5-tests.ps1` +43 บรรทัด | 🔴 gate self-certification — **T1/T2 ยังเปิดอยู่** ดู §1 |
| `echo_server.py` +45 บรรทัด · `Wire.mqh` +64 | scripted server + partial-send (ข้อ 1.6) |

⚠️ **ทั้งหมดยัง uncommitted และยังไม่ผ่าน review** — อย่านับเป็นเสร็จ

### สิ่งที่ยังไม่มีเลยแม้แต่ไฟล์เดียว

| | ผลกระทบ |
|---|---|
| **git remote** | ไม่มี backup · ไม่มี CI · เครื่องพังคือหายหมด (D16) |
| **`pyproject.toml` · `tools/task.py` · `.env.example`** | ฝั่ง Python ยังเริ่มไม่ได้ (SPEC-002) |
| **`brain/`** (นอกจาก `echo_server.py`) | gateway · risk · signal ยังไม่มี |
| **DB** | ยังไม่ติดตั้ง PostgreSQL (D13) · ยังไม่มี migration |
| **`contracts/gen/`** | codegen ยังไม่รัน — pydantic/MQL5 struct ยังไม่มีสักตัว |

---

## 2. แผน Codex — 12 รอบที่เหลือ เรียงตามลำดับบังคับ

ลำดับเต็มพร้อมกับดักรายตัวอยู่ใน [`work-order.md`](work-order.md) — ตารางนี้คือแผนที่

| รอบ | ticket | test | ขึ้นกับ | หมายเหตุ |
|-----|--------|------|---------|----------|
| **1** 🔨 | ปิด SPEC-001 (chaos A · partial send · soak) | 6+ | — | **กำลังทำ** |
| **2** 🔨 | SPEC-063 BrokerTime | 15 | 001 | **กำลังทำ** — ปิด G6 |
| **0** | SPEC-002 scaffold | 9 | — | แทรกได้ทุกเมื่อ · **ต้องมาก่อนรอบ 3** |
| **3** ★ | SPEC-004 Codegen | 27 | 002 | ก้อนใหญ่สุด · `JsonCore.mqh` เป็นตัวยาก |
| **5** | SPEC-064 SymbolRegistry | 20 | 004 | ปิด G7 |
| **5.5** | SPEC-005 Round-trip | 17 | 002·004 | **exit criteria Phase 0** |
| **4** | SPEC-010 StateReporter | 24 | 004·063 | backfill pacing |
| **5.6** | SPEC-006 Database | 16 | 002·004 | เขียนได้เลย · รัน test ต้องมี D13 |
| **5.7** ★ | SPEC-013 Gateway | 27 | 002·004·006·064 | **คอขวดของ Phase 1** |
| **5.8** | SPEC-007 Data ingest | 18 | 002·064 | `resample.py` เริ่มได้ก่อนไม่ต้องมี MT5 |
| **5.9** | SPEC-008 Quality gate | 22 | — | ข้อมูลสังเคราะห์ล้วน · แทรกได้ |
| — | SPEC-011 OrderRouter ★ | — | 010 + ข้อ 1.6 | ตัวยากสุดของ Phase 1 |
| **5.10** | SPEC-014 Persistence | 18 | 006·013 | "ไม่มีหลักฐาน = ไม่เทรด" |
| **5.11** | SPEC-017 Reconciliation | 15 | 010·011·063 | **exit criteria Phase 1** |
| **5.12** | SPEC-012 Intent cache | 15 | 011·063 | เล็กสุด |
| **5.13** | SPEC-018 Consistency checker | 15 | 014·064 | **exit criteria Phase 1** |
| **5.14** | SPEC-015 EMA baseline | 20 | 013·014·064 | พิสูจน์ท่อ ไม่ใช่ทำเงิน |
| **5.15** | SPEC-065 + SPEC-009 | — | 001·002 | **ปิด Phase 0** · ปิด G3/G9 |
| **7** ★★ | SPEC-019…023 risk L1 | ~80 | 011·012·063·064 | **ชั้นที่เงินหาย** |
| **8** | SPEC-060 + SPEC-061 | 28 | 006·013·064 | ปิด G1 (fail-open) |
| **9** | SPEC-027 RISK_DIRECTIVE | 28 | 013·019·023·063 | ★ **ไม่ต้องรอ D9/D10** · matrix 25 ช่อง |
| **10** | SPEC-028 dashboard + kill switch | 17 | 006·014·023 | **ปิดเกณฑ์ Phase 2 exit "flatten ≤ 5s"** |
| **11** | SPEC-029 Telegram | 19 | 006·014 | 🔑 ต้องมี bot token ตอนรัน test ชั้น live |
| **12** | SPEC-062 watchdog | 17 | 028·029 | ปิด G2 · stdlib ล้วน ≤ 300 บรรทัด |

**ทางวิกฤตถึง Phase 1: `002 → 004 → 006 → 013`** — ไม่มี gateway = ทดสอบ end-to-end ไม่ได้เลย
**ทางวิกฤตถึงเงินจริง: บวก `010 → 011 → 019…023 → 060/061 → 024…027`**

### งานที่แทรกได้ตอน blocked (ไม่มี dependency)

`SPEC-008` (ข้อมูลสังเคราะห์) · `SPEC-007 resample.py` · `SPEC-015 sizing.py` · `SPEC-018 checker`
— ทั้ง 4 เป็นฟังก์ชันบริสุทธิ์ เขียน + test ได้โดยไม่ต้องมี MT5/DB/gateway

---

## 3. แผน Claude — เขียนหมดทุกฉบับที่เขียนได้แล้ว

**Phase 0 · 1 · และครึ่ง Phase 2 ปิดครบ** · ✅ rev.3 เพิ่ม **SPEC-027 · 028 · 029 · 062**

| ลำดับ | spec | สถานะ | ติดอะไร |
|-------|------|-------|---------|
| ~~1~~ | ~~SPEC-027 RISK_DIRECTIVE~~ | ✅ **เขียนแล้ว** | — |
| ~~2~~ | ~~SPEC-028 dashboard + kill switch~~ | ✅ **เขียนแล้ว** | — |
| ~~3~~ | ~~SPEC-029 Telegram~~ | ✅ **เขียนแล้ว** | — |
| ~~4~~ | ~~SPEC-062 watchdog~~ | ✅ **เขียนแล้ว** | — |
| 5 | **SPEC-024** currency exposure (P3) | 🔴 เขียนไม่ได้ | **D9** |
| 6 | **SPEC-025** `brain/risk/` P1…P11 | 🔴 เขียนไม่ได้ | **D9 + D10** |
| 7 | **SPEC-026** correlation + P4 | 🔴 เขียนไม่ได้ | **D9** |
| 8 | **SPEC-030** risk scenario 20 สถานการณ์ | ⏳ รอของจริง | ต้องมี 019–027 ทำงานก่อน |
| 9 | **SPEC-030b** runbook ขั้นต้น (G4) | ⏳ รอของจริง | ต้องมี 023 + 028 ทำงานก่อน |
| 10 | Phase 3 — SPEC-031…039 | ⬜ | 🔴 **D15** บล็อก 032/033 |
| 11 | Phase 4 — SPEC-040…045 | ⬜ | — |
| 12 | Phase 5 — SPEC-046…050 | ⬜ | — |
| 13 | Phase 6 — SPEC-051…056 | ⬜ | D7 (VPS) |
| 14 | Phase 7 — SPEC-057…059 | ⬜ | — |

### ★ SPEC-027 กลับด้าน dependency — ปลดล็อกได้ฟรี

backlog เดิมให้ SPEC-027 ขึ้นกับ SPEC-025 ซึ่งทำให้มันติดอยู่หลัง D9/D10 ไปด้วย

| | ขอบเขตใหม่ |
|---|-----------|
| **SPEC-027** | **ท่อ + กฎ precedence** — `effective = max(local, directive)` |
| **SPEC-025** | **คนตัดสิน** — P1…P11 ดูตัวเลขแล้วเลือกว่าจะส่ง directive อะไร |

กฎ `max()` ไม่ขึ้นกับว่า P3 วัดเป็น notional หรือ risk-normalized เลย
→ **SPEC-027 ทำได้ทันที** และ SPEC-025 จะ `import` จากมัน (ห้ามเขียน `stricter_of` ใหม่)

> **ยังสำคัญเหมือนเดิม:** ลำดับ 5–7 เขียนไม่ได้จนกว่า D9/D10 จะตอบ — ไม่ใช่ "เขียนไปก่อนแล้วแก้"
> เพราะทั้งสองข้อ**เปลี่ยนความหมายของกฎ risk** ไม่ใช่แค่ปรับตัวเลข
>
> **ลำดับ 8–9 ต่างออกไป:** เขียนได้ในทางเทคนิค แต่จะกลายเป็นเอกสารที่แต่งจากจินตนาการ
> · runbook ที่เขียนก่อนเคยกดปุ่มจริงคือ runbook ที่ผิดตอนต้องใช้ · **รอของจริงถูกแล้ว**

---

## 4. แผนเจ้าของ — สิ่งที่ไม่มีใครทำแทนได้

### 4.1 การตัดสินใจที่บล็อกงานอยู่

| # | เรื่อง | บล็อกอะไร | ต้องตอบเมื่อไร |
|---|-------|-----------|---------------|
| **D16** ⭐ | สร้าง git remote ที่ไหน | backup ทั้งโปรเจกต์ · CI ชั้น 3 | **วันนี้** — ความเสี่ยงสูงสุดต่อความพยายามต่ำสุด |
| **D13** | ติดตั้ง PostgreSQL อย่างไร (แนะนำ native Windows) | รัน test ของ SPEC-006/014 | ก่อนรอบ 5.6 |
| **D9** | หน่วยของ P3/P4 — notional หรือ risk-normalized | SPEC-024 · 025 · 026 | **ก่อน Claude เขียน spec ตัวถัดไป** |
| **D10** | hedge ข้ามบัญชี: ห้ามทั้งฟาร์ม หรือแยกบัญชีแล้วสวนได้ | SPEC-025 | เดียวกับ D9 |
| **D17** | self-hosted runner บนเครื่องนี้ไหม | CI รัน MQL5 อัตโนมัติ (แทน attestation) | หลัง D16 |
| **D11** | XM login ไม่ผ่าน | ทุกอย่างที่เกี่ยวกับ XM | ก่อน Phase 2 exit (ดู §5) |
| **D15** | historical broker→UTC mapping | SPEC-032 · 033 | ก่อน Phase 3 |
| **D7** | สเปก VPS | SPEC-032 spread model · Phase 6 | 🟡 |
| **D12** | contract spec ของ XM (อาจพลิก D5) | ขนาดทุน live | 🟡 รอ D11 |
| **D14** | เอา TimescaleDB ไหม (แนะนำ: ยังไม่เอา) | — | 🟡 ประตูเปิดกลับได้ |

### 4.2 ของที่ต้องซื้อ/ตั้งค่านอกโค้ด — **เริ่มหาไว้ก่อน อย่ารอถึงตอนนั้น**

VPS (D7) · Telegram bot token (SPEC-029) · บัญชี demo ที่ 2–3 (Phase 2 exit) · ทุน live (D5)

### 4.3 งานมือที่ต้องทำครั้งเดียวแต่บล็อกอยู่ตอนนี้

**socket whitelist ในเทอร์มินัล MT5** — `Tools → Options → Expert Advisors` → เพิ่ม `127.0.0.1`
`config\settings.ini` เป็นไฟล์เข้ารหัส **แก้ด้วย script ไม่ได้** → ถ้ายังไม่ได้ตั้ง
chaos harness (รอบ 1.5) จะรันไม่ผ่านและ Codex จะติดตรงนี้

---

## 5. เวลาที่บีบไม่ได้ — soak run

นี่คือส่วนที่**ไม่ขึ้นกับความเร็วของใครเลย** และมักถูกลืมตอนวางแผน

| ประตู | ต้องรัน | บีบได้ไหม |
|-------|---------|-----------|
| SPEC-001 รอบ 1.7 | soak 24 ชม. | ❌ |
| **Phase 1 exit** | demo 1 บัญชี **5 วันทำการติดกัน** | ❌ |
| **Phase 2 exit** | demo **3 บัญชีขนาน 10 วันทำการ** | ❌ |
| Phase 7.1 | live 10% **30 วัน** | ❌ |

**รวม ~3 สัปดาห์ปฏิทินก่อนถึงเงินจริง** ที่เขียนโค้ดเร็วแค่ไหนก็ไม่ช่วย
→ **เริ่ม soak ทันทีที่ของพร้อม อย่ารอให้ ticket อื่นเสร็จก่อน**

> 🔴 **ข้อที่ยังไม่มีคำตอบ:** Phase 2 exit ขอ **3 บัญชี demo ขนาน** — ตอนนี้ XM ใช้ไม่ได้ (D11)
> ถ้าใช้ IUX 3 บัญชี จะไม่ได้พิสูจน์ **SymbolRegistry ข้ามโบรกเกอร์** และ **P12 cross-account hedge**
> ซึ่งเป็นสองอย่างที่ ADR-003 เพิ่มเข้ามาโดยเฉพาะ
> **ต้องตัดสินก่อนถึงประตูนั้น** — จะแก้ D11 ให้ได้ หรือยอมรับว่าประตูนี้พิสูจน์ไม่ครบ

---

## 6. ประตูที่ต้องผ่าน — สรุปจาก [roadmap](04-roadmap.md)

| Phase | เกณฑ์ที่คนมักมองข้าม |
|-------|---------------------|
| **0** | round-trip py↔mql5 ตรงทุก message type · M1 ≥ 5 ปี ครบ 6 คู่ missing < 0.1% |
| **1** | intent เดียวกัน 100 ครั้ง → 1 order · kill brain → SafeMode ≤ 10s · latency p95 < 500ms |
| **2** ★ | R1–R17 + P1–P11 มี test ครบ · kill switch flatten ≤ 5s · **SL เป็น broker-side** (G5) · watchdog (G2) · runbook (G4) · **Claude อนุมัติเป็นลายลักษณ์อักษร** |
| **3** | backtest↔live parity ≥ 99% · leakage test (shuffle label → Sharpe ≈ 0) |
| **4** | regime filter ลด maxDD ≥ 20% **ไม่งั้นไม่ใช้** |
| **5** | promotion ต้องมีมนุษย์อนุมัติ ห้าม auto |
| **6** | 5+ terminal stable 10 วัน · restore บนเครื่องเปล่าสำเร็จ |
| **7** | ramp 10→25→50→100% · DD ถึง 60% ของ limit → ลดทุนกลับ ไม่ต้องถาม |

---

## 7. ประเมินเวลา

**ยังไม่ให้ตัวเลขสัปดาห์** — Codex ยังไม่ปิด ticket ที่มี spec เต็มรูปแบบสักตัว
(SPEC-001 เขียนก่อนมีโปรโตคอลนี้) **หลังปิดรอบ 3 (SPEC-004, 27 test) จะประเมินได้แม่นจริง**

สิ่งที่บอกได้ตอนนี้:

| | |
|---|---|
| งานที่ *พร้อมทำ* | **31 ticket · ~570 test** |
| งานที่ต้องออกแบบก่อน | **34 spec** — แต่ **0 ฉบับที่ Claude เขียนได้ตอนนี้** (3 ติด D9/D10 · 2 รอของจริง · 29 อยู่ Phase 3+) |
| เวลาปฏิทินที่บีบไม่ได้ | ~3 สัปดาห์ (§5) |
| ของที่ต้องรื้อ | **ยังไม่มี** — ลำดับที่เลือกมาถูกแล้ว |

---

## 8. สรุปสั้นที่สุด

**ทำต่อทันที:**

- **Codex** → **① แก้ gate (T1 + T2) แล้วรันใหม่ทั้งหมด ผลเก่าทิ้ง** → ② B4 throttle 20s
  → ③ B15/B13/B9 (แก้ที่เดียวกัน) → ④ ปิดรอบ 1 chaos (C1–C13) → แทรกรอบ 0 (scaffold) → รอบ 3
- **Claude** → **ไม่มี spec ที่เขียนได้ค้างอยู่แล้ว** · งานตอนนี้คือ **review รอบถัดไป** ทั้งหมด
- **เจ้าของ** → **git remote (D16) วันนี้** · ตอบ **D9 + D10** (ปลดล็อก 3 ticket)
  · ตั้ง socket whitelist ในเทอร์มินัล · ตัดสิน D13 · เริ่มหา **Telegram bot token** (SPEC-029)

**ยังไม่ต้องคิดตอนนี้:** Phase 4 ขึ้นไป — ไกลเกินกว่าจะวางแผนละเอียดได้อย่างมีประโยชน์

> ⚠️ **ข้อที่เปลี่ยนไปจาก rev.2:** ตอนนั้นคอขวดคือ "Claude เขียน spec ไม่ทัน"
> ตอนนี้ **spec ไม่ใช่คอขวดอีกแล้ว** — คอขวดคือ **ความน่าเชื่อถือของ gate**
> เพราะถ้า gate เชื่อไม่ได้ การ implement 31 ticket ข้างหน้าจะสร้างหนี้ที่มองไม่เห็น
> เร็วกว่าที่เราจะตรวจเจอ · **นี่คือเหตุผลที่ T1/T2 อยู่ก่อนทุกอย่าง**
