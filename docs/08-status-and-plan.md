# 08 — สถานะจริง + แผนที่เหลือ

**อัปเดต:** 2026-07-31 (rev.5) · เอกสารนี้ตอบคำถามเดียว: **"เหลืออะไรอีก"**
ตัวเลขทั้งหมดนับจาก `backlog.md` และไฟล์จริง ไม่ใช่ประมาณการ

> **rev.5 แก้อะไร:**
> · **คอขวดเปลี่ยนตัวอีกครั้ง** — rev.4 บอกว่าคือ "gate เชื่อไม่ได้" · **T1/T2 ปิดแล้ว gate เชื่อได้แล้ว**
>   ตอนนี้คือ **เวลาปฏิทิน (soak) + งาน gate 7 ข้อที่ยังไม่ถูกแตะ**
> · **SPEC-063 ปิด** `APPROVED_WITH_NOTES` ([review 04](reviews/SPEC-063-04.md)) → เสร็จจริง 1 → **2**
> · **SPEC-016 ชั้น A ปิด** `APPROVED_WITH_NOTES` ([review chaos 03](reviews/SPEC-016-chaos-03.md)) · **W1 เจอต้นเหตุจริงแล้ว**
> · ticket รวม 66 → **67** (เพิ่ม SPEC-066 handoff doorbell)
> · **working tree สะอาด** — ของทั้งหมดที่ rev.4 เตือนว่า "ยัง uncommitted" commit ครบแล้วถึง `70aba53`
> · **socket whitelist ปิดแล้ว** — หลุดจากรายการงานมือของเจ้าของ
>
> <details><summary>rev.4</summary>
>
> · **D9 + D10 ปิดแล้ว** → [ADR-004](decisions/ADR-004-exposure-unit.md) · [ADR-005](decisions/ADR-005-cross-account-hedge.md)
> · spec พร้อมเพิ่มจาก 31 → 34 (เขียน SPEC-024 · 025 · 026 เสร็จ)
> · ไม่มี spec ที่ติดการตัดสินใจของเจ้าของเหลืออยู่แล้ว
> </details>
>
> <details><summary>rev.3</summary>
>
> · spec พร้อมเพิ่มจาก 27 → 31 (เขียน SPEC-027 · 028 · 029 · 062 เสร็จ)
> · **SPEC-027 กลับด้าน dependency** — ไม่ขึ้นกับ SPEC-025 แล้ว จึงหลุดจากเงา D9/D10
> · SPEC-063 เข้ารอบ review ที่ 2 → `CHANGES_REQUIRED` · เจอปัญหาที่ gate ซึ่งใหญ่กว่าตัวโค้ด
> </details>

---

## 1. ตอนนี้อยู่ตรงไหน

| | จำนวน |
|---|---|
| ticket ทั้งหมด | **67** |
| ✅ เสร็จจริง (implement + review ผ่าน) | **2** — SPEC-003 schema · **SPEC-063 BrokerTime** |
| 🔨 กำลังทำ | **2** — SPEC-001 (เหลือ soak) · SPEC-016 (ชั้น A ผ่าน · ชั้น B รอ 011+013) |
| 📄 มี spec พร้อมให้ Codex ทำ | **32** |
| 🔴 มี spec ไม่ได้เพราะรอเจ้าของ | **0** ✅ |
| ⬜ ยังไม่มี spec (Phase 3 ขึ้นไป + 030/030b) | **31** |

**งานที่เสร็จจริง ~3% · งานที่ *พร้อมทำทันที* ~51%**

Codex มีงานในมือ **~600 test ข้าม 32 ticket** (นับจากตาราง §2 ได้ 557 · อีก 4 ticket ยังไม่ระบุจำนวนใน spec)

### ✅ เรื่องที่เคยเป็นเรื่องด่วนที่สุดใน rev.4 — **ปิดแล้ว**

rev.4 ขึ้นหัวว่า *"gate เชื่อไม่ได้"* · ตอนนี้ยืนยันจากผลรันจริงว่า **เชื่อได้แล้ว**

| finding เดิม | สถานะ |
|---|---|
| `.ex5` + compile log ไม่ถูกลบก่อน compile | ✅ ปิด (T1) |
| `Result: N errors` พิมพ์แต่ไม่ตรวจ | ✅ ปิด (T1) |
| `$RequiredSuiteNames` ไม่มี `ContainsKey` guard | ✅ ปิด (T2 · `run-mql5-tests.ps1:261-265`) |
| `git_sha` ฉีดผ่าน `.set` จับ stale binary ไม่ได้ | ✅ ยืนยันตรงจริงที่ commit `294da01` |

ผลรันล่าสุด: `compile 0 error 0 warning` · `TestWire 53/53` · `TestBrokerTime 67/67` · `CHAOS GATE: PASSED`

### 🔴 เรื่องด่วนที่สุดตอนนี้ — **2 อย่าง และคนละชนิดกัน**

**① soak ที่ยังไม่ได้เริ่ม — บีบไม่ได้ ยิ่งเริ่มช้ายิ่งเลื่อนทั้งโปรเจกต์**

| | |
|---|---|
| soak 24 ชม. ([SPEC-001 §1.7](work-order.md)) | 🔴 ยังไม่ได้รัน |
| slow gate `test_no_heartbeat_loss_over_1h` | 🔴 ยังไม่ได้รัน (1 ชม.) |

**สุดสัปดาห์นี้คือหน้าต่างที่ถูกที่สุด** — ตลาดปิด · EA เป็น timer-driven จึงรันได้โดยไม่ต้องรอ tick
· ปล่อยรันแล้วทำ ticket อื่นขนานไปได้ · **การรอให้ ticket อื่นเสร็จก่อนแล้วค่อย soak คือการเสียเวลาฟรี**

**② `แก้ 5` — งาน gate 7 ข้อที่ยัง *ไม่ถูกแตะเลย* (ตรวจโค้ดยืนยัน 2026-07-31)**

| # | ยืนยันจาก | สภาพ |
|---|-----------|------|
| **P1** preflight (พอร์ต · terminal ค้าง · exe มีจริง) | ไม่มีคำว่า preflight ใน `run-chaos.ps1` เลย | ❌ |
| **⑤** `exit 3` แยก env failure | ไม่มี | ❌ |
| **T7** `ReadToEnd()` 2 สตรีมเรียงกัน → deadlock ได้ | `run-chaos.ps1:37-38` | ❌ |
| **T9** ไม่มี timeout รวม + ไม่เก็บกวาด process ลูก | `:39` `WaitForExit()` เปล่า | ❌ |
| **T8** `$Repo = "D:\ea-farm"` hardcoded 3 ที่ | `run-chaos:14` · `compile-gate:18` · `run-mql5-tests:34` | ❌ |
| **N1(gate)** `*-observed.json` เขียน `git_sha` ของ HEAD ก่อนเช็คผล | ไม่มี `git_sha_head` ในโค้ด | ❌ |
| **T6** เช็ครายชื่อ test ที่รันจริง ไม่ใช่นับจำนวน | regex อ่านค่าตอน fail ได้แล้ว แต่ยังนับจำนวน | 🟠 ครึ่งเดียว |

**T9 มีหลักฐานสดว่ากัดจริง** — รอบ 2026-07-30 ค้างเกิน 10 นาทีจนถูกตัด
แล้วทิ้ง `terminal64.exe` + `python` ค้าง 3 process เพราะ `finally` ของ harness ไม่ทำงานตอนแม่ถูก kill

> ทั้งสองข้อนี้ **ต่างชนิดกันโดยสิ้นเชิง** — ① ซื้อด้วยเงินหรือความขยันไม่ได้ ต้องเริ่มเร็ว
> ② เป็นงานเขียนโค้ดธรรมดาที่ทำได้ทันที · **ทำ ① ให้เริ่มรันก่อน แล้วค่อยลง ②** ระหว่างรอ

### หลักฐานสถานะปัจจุบัน (branch `feat/SPEC-001-mt5-executor`)

**working tree สะอาด** — ต่างจาก rev.4 ที่ทุกอย่างยัง uncommitted

| commit | คือ |
|--------|-----|
| `50bab8d` | **ต้นเหตุจริงของ W1** — `SocketRead` ขอ 4096 byte เสมอทั้งที่ `SocketIsReadable()` คืนจำนวน byte → บล็อก `Pump()` **29 วินาที** |
| `461b69a` | diag file + `FileFlush()` — **เครื่องมือที่ทำให้ W1 จบ ต้องเก็บถาวร** |
| `262c28d` | deferred HELLO (แยก connect/send คนละ `Pump()`) |
| `70aba53` | review chaos รอบ 3 — `APPROVED_WITH_NOTES` |

`pump_us` สูงสุด: **29,031,694 → 16,576 µs** · เกณฑ์ p99 < 20,000 ✅ **ผ่านด้วยของจริง ไม่ใช่ mock**

### สิ่งที่ยังไม่มีเลยแม้แต่ไฟล์เดียว

| | ผลกระทบ |
|---|---|
| **git remote** | ไม่มี backup · ไม่มี CI · เครื่องพังคือหายหมด (D16) — **ค้างมา 3 วันแล้ว** |
| **`pyproject.toml` · `tools/task.py` · `.env.example`** | ฝั่ง Python ยังเริ่มไม่ได้ (SPEC-002) |
| **`brain/`** (นอกจาก `echo_server.py`) | gateway · risk · signal ยังไม่มี |
| **DB** | ยังไม่ติดตั้ง PostgreSQL (D13) · ยังไม่มี migration |
| **`contracts/gen/`** | codegen ยังไม่รัน — pydantic/MQL5 struct ยังไม่มีสักตัว |

---

## 2. แผน Codex — เรียงตามลำดับบังคับ

ลำดับเต็มพร้อมกับดักรายตัวอยู่ใน [`work-order.md`](work-order.md) — ตารางนี้คือแผนที่

| รอบ | ticket | test | ขึ้นกับ | หมายเหตุ |
|-----|--------|------|---------|----------|
| **1** 🔨 | ปิด SPEC-001 — soak + slow gate + N1 + W2 | — | — | **กำลังทำ** · เหลือแค่ 4 ข้อ ดู §2.1 |
| ~~**2**~~ | ~~SPEC-063 BrokerTime~~ | ~~15~~ | — | ✅ **ปิดแล้ว** — G6 ปิด |
| **0** | SPEC-002 scaffold | 9 | — | แทรกได้ทุกเมื่อ · **ต้องมาก่อนรอบ 3** · T8 จะไปจบที่นี่ |
| **3** ★ | SPEC-004 Codegen | 27 | 002 | ก้อนใหญ่สุด · `JsonCore.mqh` เป็นตัวยาก |
| **5** | SPEC-064 SymbolRegistry | 20 | 004 | ปิด G7 |
| **5.5** | SPEC-005 Round-trip | 17 | 002·004 | **exit criteria Phase 0** |
| **4** | SPEC-010 StateReporter | 24 | 004·063 ✅ | backfill pacing · **dependency ปลดล็อกครบแล้ว** |
| **5.6** | SPEC-006 Database | 16 | 002·004 | เขียนได้เลย · รัน test ต้องมี D13 |
| **5.7** ★ | SPEC-013 Gateway | 27 | 002·004·006·064 | **คอขวดของ Phase 1** |
| **5.8** | SPEC-007 Data ingest | 18 | 002·064 | `resample.py` เริ่มได้ก่อนไม่ต้องมี MT5 |
| **5.9** | SPEC-008 Quality gate | 22 | — | ข้อมูลสังเคราะห์ล้วน · แทรกได้ |
| — | SPEC-011 OrderRouter ★ | — | 010 + §1.6 ✅ | ตัวยากสุดของ Phase 1 · `test_partial_send_resumes` ปิดแล้ว |
| **5.10** | SPEC-014 Persistence | 18 | 006·013 | "ไม่มีหลักฐาน = ไม่เทรด" |
| **5.11** | SPEC-017 Reconciliation | 15 | 010·011·063 ✅ | **exit criteria Phase 1** |
| **5.12** | SPEC-012 Intent cache | 15 | 011·063 ✅ | เล็กสุด |
| **5.13** | SPEC-018 Consistency checker | 15 | 014·064 | **exit criteria Phase 1** |
| **5.14** | SPEC-015 EMA baseline | 20 | 013·014·064 | พิสูจน์ท่อ ไม่ใช่ทำเงิน |
| **5.15** | SPEC-065 + SPEC-009 | — | 001·002 | **ปิด Phase 0** · ปิด G3/G9 · **attestation มีความหมายแล้วเพราะ gate เชื่อได้** |
| — | SPEC-016 **ชั้น B** (intent chaos) | 5 scenario | 011·013 | ชั้น A ปิดแล้ว |
| **7** ★★ | SPEC-019…023 risk L1 | ~80 | 011·012·063 ✅·064 | **ชั้นที่เงินหาย** |
| **8** | SPEC-060 + SPEC-061 | 28 | 006·013·064 | ปิด G1 (fail-open) |
| **9** | SPEC-027 RISK_DIRECTIVE | 28 | 013·019·023·063 ✅ | matrix 25 ช่อง |
| **10** | SPEC-028 dashboard + kill switch | 17 | 006·014·023 | **ปิดเกณฑ์ Phase 2 exit "flatten ≤ 5s"** |
| **11** | SPEC-029 Telegram | 19 | 006·014 | 🔑 ต้องมี bot token ตอนรัน test ชั้น live |
| **12** | SPEC-062 watchdog | 17 | 028·029 | ปิด G2 · stdlib ล้วน ≤ 300 บรรทัด |
| **13** ★ | SPEC-024 currency exposure (P3) | 28 | 060·061·064·013 | ฐานตัวเลขของ 025/026 · [ADR-004](decisions/ADR-004-exposure-unit.md) |
| **14** | SPEC-026 correlation + P4 | 24 | 024·060 | ปิด G1 ฝั่ง P4 (`corr = 1.0` เมื่อไม่รู้) |
| **15** ★★ | SPEC-025 portfolio risk P1…P12 | ~53 | 024·026·027·014 | **ปิดชั้น L2** · P12 ห้าม hedge ข้ามบัญชี ([ADR-005](decisions/ADR-005-cross-account-hedge.md)) |
| — | SPEC-066 handoff doorbell | ~40 บรรทัด | — | ของชั่วคราว · แทรกได้ · ลบทิ้งเมื่อ SPEC-029 §4.2.1 ขึ้น |

**ทางวิกฤตถึง Phase 1: `002 → 004 → 006 → 013`** — ไม่มี gateway = ทดสอบ end-to-end ไม่ได้เลย
**ทางวิกฤตถึงเงินจริง: บวก `010 → 011 → 019…023 → 060/061 → 024…027`**

### 2.1 เหลืออะไรก่อนปิด SPEC-001 — 4 ข้อ

| # | งาน | ชนิด |
|---|-----|------|
| **soak 24 ชม.** | ยังไม่ได้รัน | ⏳ เวลาปฏิทิน |
| **slow gate** `test_no_heartbeat_loss_over_1h` | ยังไม่ได้รัน | ⏳ 1 ชม. |
| **N1** | `_wait_for_backoff_ladder_connects()` คืนเฉพาะ window ที่ผ่านเกณฑ์แล้ว → **assertion เป็น dead code** · ถ้า backoff พังจะ timeout 100 วินาทีแทนที่จะล้มพร้อมตัวเลข | 🟠 แก้ ~5 บรรทัด |
| **W2** | `echo_server` นับ TCP ขยะ (chrome ต่อ `:45001` ทุก 60 วินาที) เป็น `connect` → flaky ตามว่าเปิดเบราว์เซอร์อยู่ไหม | 🟠 |

**ไม่มีบั๊กที่ยังหาไม่เจอแล้ว** — ทั้ง 6 chaos test ผ่านด้วยพฤติกรรมจริง
· backoff พิสูจน์ครบ **ถึง cap 30 วินาที** ไม่ใช่แค่ผ่านเกณฑ์หลวมๆ

### งานที่แทรกได้ตอน blocked (ไม่มี dependency)

`SPEC-008` (ข้อมูลสังเคราะห์) · `SPEC-007 resample.py` · `SPEC-015 sizing.py` · `SPEC-018 checker`
— ทั้ง 4 เป็นฟังก์ชันบริสุทธิ์ เขียน + test ได้โดยไม่ต้องมี MT5/DB/gateway
· บวก **`แก้ 5` ทั้งชุด** ซึ่งไม่ขึ้นกับอะไรเลยและควรทำระหว่าง soak รัน

---

## 3. แผน Claude — เขียนหมดทุกฉบับที่เขียนได้แล้ว

**Phase 0 · 1 · และครึ่ง Phase 2 ปิดครบ** · rev.5 ไม่มีอะไรเพิ่ม — สถานะเดิมจาก rev.4

| ลำดับ | spec | สถานะ | ติดอะไร |
|-------|------|-------|---------|
| ~~1–7~~ | ~~SPEC-024 · 025 · 026 · 027 · 028 · 029 · 062~~ | ✅ **เขียนแล้ว** | — |
| ~~—~~ | ~~SPEC-066 handoff doorbell~~ | ✅ **เขียนแล้ว** (rev.5) | — |
| 8 | **SPEC-030** risk scenario 20 สถานการณ์ | ⏳ รอของจริง | ต้องมี 019–027 ทำงานก่อน |
| 9 | **SPEC-030b** runbook ขั้นต้น (G4) | ⏳ รอของจริง | ต้องมี 023 + 028 ทำงานก่อน |
| 10 | Phase 3 — SPEC-031…039 | ⬜ | 🔴 **D15** บล็อก 032/033 |
| 11 | Phase 4 — SPEC-040…045 | ⬜ | — |
| 12 | Phase 5 — SPEC-046…050 | ⬜ | — |
| 13 | Phase 6 — SPEC-051…056 | ⬜ | D7 (VPS) |
| 14 | Phase 7 — SPEC-057…059 | ⬜ | — |

> **ลำดับ 8–9 เขียนได้ในทางเทคนิค แต่จะกลายเป็นเอกสารที่แต่งจากจินตนาการ**
> runbook ที่เขียนก่อนเคยกดปุ่มจริงคือ runbook ที่ผิดตอนต้องใช้ · **รอของจริงถูกแล้ว**
>
> ของที่ **จะ** เข้า SPEC-030b แล้วตอนนี้: ขั้นตอน whitelist MT5 5 ขั้น
> (ดู [work-order §P1](work-order.md)) — ★ ข้อ 5 *"ปิดด้วย `File → Exit`"* คือข้อที่ทำให้เสียเวลาไปทั้งคืน

**งานของ Claude ตอนนี้ = review รอบถัดไปทั้งหมด** — ไม่มี spec ค้างเขียนแม้แต่ฉบับเดียว

---

## 4. แผนเจ้าของ — สิ่งที่ไม่มีใครทำแทนได้

### 4.1 การตัดสินใจที่บล็อกงานอยู่

| # | เรื่อง | บล็อกอะไร | ต้องตอบเมื่อไร |
|---|-------|-----------|---------------|
| **D16** ⭐ | สร้าง git remote ที่ไหน | backup ทั้งโปรเจกต์ · CI ชั้น 3 | 🔴 **ค้างมา 3 วัน** — ความเสี่ยงสูงสุดต่อความพยายามต่ำสุด |
| **D13** | ติดตั้ง PostgreSQL อย่างไร (แนะนำ native Windows) | รัน test ของ SPEC-006/014 | ก่อนรอบ 5.6 |
| **D17** | self-hosted runner บนเครื่องนี้ไหม | CI รัน MQL5 อัตโนมัติ (แทน attestation) | หลัง D16 |
| **D11** | XM login ไม่ผ่าน | ทุกอย่างที่เกี่ยวกับ XM | ก่อน Phase 2 exit (ดู §5) |
| **D15** | historical broker→UTC mapping | SPEC-032 · 033 | ก่อน Phase 3 |
| **D7** | สเปก VPS | SPEC-032 spread model · Phase 6 | 🟡 |
| **D12** | contract spec ของ XM (อาจพลิก D5) | ขนาดทุน live | 🟡 รอ D11 |
| **D14** | เอา TimescaleDB ไหม (แนะนำ: ยังไม่เอา) | — | 🟡 ประตูเปิดกลับได้ |
| ~~D9~~ · ~~D10~~ | ~~หน่วย P3/P4~~ · ~~hedge ข้ามบัญชี~~ | ✅ **ปิด 2026-07-30** → [ADR-004](decisions/ADR-004-exposure-unit.md) · [ADR-005](decisions/ADR-005-cross-account-hedge.md) | — |

### 4.2 ของที่ต้องซื้อ/ตั้งค่านอกโค้ด — **เริ่มหาไว้ก่อน อย่ารอถึงตอนนั้น**

VPS (D7) · Telegram bot token (SPEC-029) · บัญชี demo ที่ 2–3 (Phase 2 exit) · ทุน live (D5)

### 4.3 ✅ งานมือที่เคยบล็อก — ปิดแล้ว

**socket whitelist ในเทอร์มินัล MT5** — ตั้งเรียบร้อยแล้ว พิสูจน์ด้วย chaos suite ที่ต่อ EA ได้จริง
· `err=4014` เหลือ **0 ครั้ง** · ขั้นตอนที่ใช้จะถูกบันทึกลง runbook SPEC-030b
· ⚠️ **เป็น state ของเครื่องที่ไม่อยู่ใน git** — เครื่องใหม่ต้องตั้งใหม่ทั้งหมด

---

## 5. เวลาที่บีบไม่ได้ — soak run

นี่คือส่วนที่**ไม่ขึ้นกับความเร็วของใครเลย** และมักถูกลืมตอนวางแผน

| ประตู | ต้องรัน | บีบได้ไหม | สถานะ |
|-------|---------|-----------|-------|
| SPEC-001 §1.7 | soak 24 ชม. | ❌ | 🔴 **ยังไม่เริ่ม — เริ่มได้แล้ววันนี้** |
| SPEC-001 slow gate | `test_no_heartbeat_loss_over_1h` | ❌ | 🔴 ยังไม่เริ่ม |
| **Phase 1 exit** | demo 1 บัญชี **5 วันทำการติดกัน** | ❌ | ⬜ |
| **Phase 2 exit** | demo **3 บัญชีขนาน 10 วันทำการ** | ❌ | ⬜ |
| Phase 7.1 | live 10% **30 วัน** | ❌ | ⬜ |

**รวม ~3 สัปดาห์ปฏิทินก่อนถึงเงินจริง** ที่เขียนโค้ดเร็วแค่ไหนก็ไม่ช่วย
→ **เริ่ม soak ทันทีที่ของพร้อม อย่ารอให้ ticket อื่นเสร็จก่อน** — ตอนนี้ **ของพร้อมแล้ว**

> 🔴 **ข้อที่ยังไม่มีคำตอบ:** Phase 2 exit ขอ **3 บัญชี demo ขนาน** — ตอนนี้ XM ใช้ไม่ได้ (D11)
> ถ้าใช้ IUX 3 บัญชี จะไม่ได้พิสูจน์ **SymbolRegistry ข้ามโบรกเกอร์** และ **P12 cross-account hedge**
> ซึ่งเป็นสองอย่างที่ ADR-003 เพิ่มเข้ามาโดยเฉพาะ
> **ต้องตัดสินก่อนถึงประตูนั้น** — จะแก้ D11 ให้ได้ หรือยอมรับว่าประตูนี้พิสูจน์ไม่ครบ

---

## 6. ประตูที่ต้องผ่าน — สรุปจาก [roadmap](04-roadmap.md)

| Phase | เกณฑ์ที่คนมักมองข้าม |
|-------|---------------------|
| **0** | round-trip py↔mql5 ตรงทุก message type · M1 ≥ 5 ปี ครบ 6 คู่ missing < 0.1% |
| **1** | intent เดียวกัน 100 ครั้ง → 1 order · kill brain → SafeMode ≤ 10s · latency p95 < 500ms |
| **2** ★ | R1–R17 + P1–P11 มี test ครบ · kill switch flatten ≤ 5s · **SL เป็น broker-side** (G5) · watchdog (G2) · runbook (G4) · **Claude อนุมัติเป็นลายลักษณ์อักษร** |
| **3** | backtest↔live parity ≥ 99% · leakage test (shuffle label → Sharpe ≈ 0) |
| **4** | regime filter ลด maxDD ≥ 20% **ไม่งั้นไม่ใช้** |
| **5** | promotion ต้องมีมนุษย์อนุมัติ ห้าม auto |
| **6** | 5+ terminal stable 10 วัน · restore บนเครื่องเปล่าสำเร็จ |
| **7** | ramp 10→25→50→100% · DD ถึง 60% ของ limit → ลดทุนกลับ ไม่ต้องถาม |

---

## 7. ประเมินเวลา

**ยังไม่ให้ตัวเลขสัปดาห์** — Codex ยังไม่ปิด ticket ที่มี spec เต็มรูปแบบสักตัวที่เป็นก้อนใหญ่
(SPEC-063 ปิดแล้วจริง แต่เป็น MQL5 ล้วน 15 test · SPEC-001 เขียนก่อนมีโปรโตคอลนี้)
**หลังปิดรอบ 3 (SPEC-004, 27 test) จะประเมินได้แม่นจริง**

สิ่งที่บอกได้ตอนนี้:

| | |
|---|---|
| งานที่ *พร้อมทำ* | **32 ticket · ~600 test** |
| งานที่ต้องออกแบบก่อน | **31 spec** — แต่ **0 ฉบับที่ Claude เขียนได้ตอนนี้** (2 รอของจริง · 29 อยู่ Phase 3+) |
| เวลาปฏิทินที่บีบไม่ได้ | ~3 สัปดาห์ (§5) |
| ของที่ต้องรื้อ | **ยังไม่มี** — ลำดับที่เลือกมาถูกแล้ว |

### 📌 ข้อมูลจริงที่เพิ่งได้จาก SPEC-016 — ใช้ประเมินรอบหน้าได้

| | |
|---|---|
| chaos fast suite รันจริง | **256 วินาที** (เคยวัดได้ 327) |
| W1 ใช้ไป **5 รอบแก้** | รอบ 1–3 เดาผิดหมด · **รอบ 4 เลิกเดาแล้วลงทุนกับ observability → รอบ 5 จบทันที** |

> **บทเรียนที่ควรเป็นกฎถาวร: เดาผิดติดกัน 2 ครั้ง → หยุดเดา แล้วลงทุนกับการมองเห็น**
> เกิดรูปแบบเดียวกันสองครั้งในวันเดียว (ไล่ W1 · ไล่ MT5 whitelist)
> ทั้งสองครั้งจบเพราะ **เห็นของจริง** ไม่ใช่เพราะเดาถูก

---

## 8. สรุปสั้นที่สุด

**ทำต่อทันที:**

- **Codex** → **① เริ่ม soak + slow gate ให้รันตั้งแต่วันนี้** (สุดสัปดาห์ = หน้าต่างที่ถูกที่สุด)
  → ② ระหว่างรอ ลง **`แก้ 5`** ทั้ง 7 ข้อ (P1 · ⑤ · T6 · T7 · T8 · T9 · N1-gate)
  → ③ N1 + W2 ปิด SPEC-001 → ④ แทรกรอบ 0 (scaffold) → รอบ 3 (SPEC-004)
- **Claude** → **ไม่มี spec ที่เขียนได้ค้างอยู่** · งานตอนนี้คือ **review รอบถัดไป** ทั้งหมด
- **เจ้าของ** → 🔴 **git remote (D16) — ค้างมา 3 วัน** · ตัดสิน D13 · เริ่มหา **Telegram bot token** (SPEC-029)

**ยังไม่ต้องคิดตอนนี้:** Phase 4 ขึ้นไป — ไกลเกินกว่าจะวางแผนละเอียดได้อย่างมีประโยชน์

> ⚠️ **คอขวดย้ายที่อีกครั้ง — ครั้งที่ 3 แล้ว**
>
> | rev | คอขวดตอนนั้น | ปิดด้วย |
> |-----|-------------|---------|
> | rev.2 | Claude เขียน spec ไม่ทัน | เขียนครบ 32 ฉบับ |
> | rev.4 | **gate เชื่อไม่ได้** | T1/T2 + ผลรันจริงที่ `git_sha` ตรง |
> | **rev.5** | **เวลาปฏิทิน (soak)** | ❌ ปิดด้วยความขยันไม่ได้ — ปิดได้ด้วยการ**เริ่มเร็ว**เท่านั้น |
>
> สองครั้งแรกแก้ด้วยการทำงานหนักขึ้น · **ครั้งนี้แก้ไม่ได้** — ทุกวันที่ไม่ได้เริ่ม soak
> คือหนึ่งวันที่บวกเข้าไปในเส้นทางถึงเงินจริงตรงๆ
