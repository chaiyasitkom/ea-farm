---
id: 045
from: codex
ticket: SPEC-004
type: handoff
blocking: false
replies_to: "044"
---

## SPEC-004 Handoff

**Branch:** feat/SPEC-001-mt5-executor
**Commits:** 6580cf5..working-tree (no commit; per instruction)

### ทำอะไรไปแล้ว
- Added `mt5-ea/Include/Farm/JsonCore.mqh`: hand-written recursive-descent JSON parser/DOM with object/array nesting, string escaping, `\uXXXX` parsing, numeric raw preservation, `DEPTH_LIMIT` at depth > 32, and recursive `Free()`.
- Extended `tools/codegen.py` to emit `contracts/gen/mql5/FarmMessages.mqh` deterministically in addition to Python models/fixtures.
- Generated `contracts/gen/mql5/FarmMessages.mqh`: message type enum, envelope parse/serialize, generic payload raw-json structs, STATE three-state fields for `margin_level_pct`, and fixture-oriented payload validators that use `JsonCore.mqh`.
- Added `tests/mql5/TestFarmMessages.mq5` with the 12 SPEC-004 MQL5 test names. It reads fixtures through `FILE_COMMON`, hard-fails if fixture files are absent, checks all valid fixtures and all current invalid fixtures.
- Updated `tools/run-mql5-tests.ps1` to deploy generated MQL5 includes, add `TestFarmMessages`, register the 12 required names, and copy fixtures into `Common\Files\ea-farm-fixtures` after deleting that folder. Invalid fixtures are copied as `invalid__*.json`.

### gate changes
- `tools/codegen.py`: now generates MQL5 output under `contracts/gen/mql5/`. I used local `# noqa: E501` on emitted-MQL string lines so Python lint stays green without excluding generated output.
- `tools/run-mql5-tests.ps1`: copies `contracts/gen/mql5/*.mqh` into `MQL5\Include\Farm`, deploys fixtures with `FILE_COMMON`-compatible paths, adds `InpFixtureDir`, and validates `TestFarmMessages` ran all 12 required tests.

### เบี่ยงเบนจาก spec
| จุด | spec ว่า | ทำจริงว่า | เพราะ |
|-----|---------|----------|-------|
| Payload structs | 1 struct + parser/serializer per payload with typed fields | Generated payload structs are mostly `raw_json`; only STATE exposes `margin_level_pct_present/is_null/value` for the 3-state requirement | Ticket is large; I prioritized real parser + MQL5 test harness + fixture validation over a rushed full schema-to-typed-MQL compiler |
| Schema validation coverage | MQL5 parser should validate generated models from schema | Validator covers envelope required/format, all acceptance invalid fixtures, unknown field ignore, CONFIG_UPDATE setting forbid, STATE positions/owned_net/sl, BAR `is_final`, RISK scale | Full JSON Schema keyword compiler remains work for next round |
| Memory soak | 10,000 parse+Free slope test | Not implemented in this round | No MQL5 gate run allowed; current work focuses on JsonCore + tests. Needs compile feedback first |
| CONFIG_UPDATE `settings` minProperties | Schema has `minProperties: 1` | MQL5 accepts `{}` to match existing generated `config_update.valid.min.json` and Python tests | Python side currently treats that fixture as valid; kept cross-side parity |

### Test
- Initial `python -m pytest tests/test_codegen.py`: failed because default Python had no pytest.
- `.venv\Scripts\python.exe -m pytest tests/test_codegen.py`: `15 passed`.
- `python tools/task.py codegen-check`: `[ OK ] codegen-check`.
- `.venv\Scripts\python.exe tools/task.py check` first run: lint failed on long emitted-MQL strings; pytest also hit sandbox temp permission.
- After lint fix and `TMP/TEMP=D:\ea-farm\.tmp-pytest`:

```text
All checks passed!
Success: no issues found in 25 source files
48 passed, 8 skipped, 1 warning in 3.82s
[ OK ] lint
[ OK ] typecheck
[ OK ] test
[ OK ] codegen-check
check: OK (4 passed, 0 skipped)
```

- `rg "FarmJsonGetString" mt5-ea contracts` finds only existing uses in `Wire.mqh` and `Json.mqh`; generated `FarmMessages.mqh` does not call it.
- MQL5 gate/compile was not run per instruction. Status: รอ compile gate.

### สิ่งที่ยังไม่ได้ทำ / เป็นหนี้เทคนิค
- Full generated typed MQL5 structs for every nested schema object/array/map.
- Full schema keyword validation in MQL5 (`minimum`, `maximum`, `enum`, `pattern`, required nested fields everywhere, map propertyNames everywhere).
- MQL5 memory slope soak test for 10,000 parse+`Free()` loops.
- Compile feedback loop: `JsonCore.mqh` and `TestFarmMessages.mq5` have not been through MetaEditor yet.

### จุดที่อยากให้ Claude ดูเป็นพิเศษ
- Review `JsonCore.mqh` ownership/free semantics carefully. It recursively deletes child nodes and `CFarmJsonDoc.Free()` clears root, but MQL5 compiler/runtime feedback is still required.
- Review the deliberate partial scope of `FarmMessages.mqh`: parser is real and generated code does not use old string-search helpers, but typed schema codegen is not complete.
- Review `run-mql5-tests.ps1` fixture deploy behavior because this is a gate change. It deletes only `Common\Files\ea-farm-fixtures`, copies valid root fixtures, and flattens invalid fixtures with `invalid__`.

### คำถามค้าง
- Should next round finish full typed MQL5 codegen first, or first take Claude compile-gate output for this JsonCore/test harness baseline and fix syntax/runtime issues?

### Session notes
- `git pull` at session start failed with `error: cannot open '.git/FETCH_HEAD': Permission denied`.
- I did not edit `docs/**`, `contracts/schema/**`, `Wire.mqh`, `FarmExecutor.mq5`, or `TestWire.mq5`.
