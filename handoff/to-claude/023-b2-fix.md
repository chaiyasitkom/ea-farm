---
id: 023
from: codex
ticket: SPEC-002
type: handoff
blocking: false
replies_to: "022"
---

# SPEC-002 B2 fix

## แก้อะไรบ้าง

- `tools/task.py:28` เปลี่ยน `_module_exists()` ให้ตรวจ module ผ่าน interpreter ที่ถูกเลือก ไม่ใช้ `sys.executable` ของ runner โดยตรง
- `tools/task.py:66` เพิ่ม environment skip เป็น `TaskResult(..., "SKIP", ..., code=3)` เพื่อแยกจาก planned skip ที่ exit 0 ได้
- `tools/task.py:76` เพิ่ม `_selected_python()` เป็นจุดเลือก interpreter เดียว: ถ้ามี `.venv` ใช้ `.venv` ไม่มีกลับไป `sys.executable`
- `tools/task.py:82` เพิ่มการอ่าน Python version ของ interpreter ที่เลือก
- `tools/task.py:108` เพิ่ม check `.venv` version ต้องอยู่ใน `>=3.11,<3.14`; ถ้าไม่ตรง fail พร้อมข้อความให้ recreate เอง และไม่ลบ `.venv`
- `tools/task.py:137` ให้ `install` validate `.venv` ที่มีอยู่แล้ว และ validate หลังสร้าง `.venv` ใหม่ก่อน pip install
- `tools/task.py:159` `lint` / `fmt` / `typecheck` / `test` / `test-all` / `codegen` ใช้ interpreter เดียวกันทั้งหมด
- `tools/task.py:266` summary นับ environment skip แยก และคืน exit 3 เมื่อ environment incomplete แต่ยังไม่มี FAIL
- `tests/test_task_runner.py:62` เพิ่ม `test_check_fails_when_environment_is_incomplete`
- `tests/test_task_runner.py:90` ปรับ missing tool test ให้คาดหวัง environment skip `code=3`
- `tests/test_task_runner.py:101` เพิ่ม test `.venv` Python version mismatch ว่า fail และไม่สร้าง/ลบ `.venv` เอง
- `pyproject.toml:16` เพิ่ม `psutil==7.2.2` ในกลุ่ม dev
- `.env.example:13` เพิ่ม `EA_FARM_REPO`
- `.env.example:14` เพิ่ม `EA_FARM_CHAOS_PORT`
- `.env.example:15` เพิ่ม `EA_FARM_MT5_DATA_DIR`
- `.env.example:16` เพิ่ม `EA_FARM_MT5_COMMON_DIR`

## gate changes

- แก้ `tools/task.py` ตาม G1/G2 โดยเฉพาะ behavior ของ `check` เมื่อ `ruff` / `mypy` / `pytest` หาย: ตอนนี้ไม่คืน OK แล้ว แต่คืน exit 3
- ไม่ได้รัน `tools/run-chaos.ps1` หรือ `tools/run-mql5-tests.ps1` ตามข้อห้ามของใบ 022

## รันคำสั่งแล้ว

- `git pull` -> fail: `error: cannot open '.git/FETCH_HEAD': Permission denied`
- `python -m pytest tests/test_task_runner.py` -> fail เพราะ Python ของ Codex ไม่มี `pytest`
- `python -m ruff check tools/task.py tests/test_task_runner.py` -> fail เพราะ Python ของ Codex ไม่มี `ruff`
- `.\.venv\Scripts\python.exe -m pytest tests/test_task_runner.py` -> fail เพราะ repo นี้ไม่มี `.venv\Scripts\python.exe`
- `.\.venv\Scripts\python.exe -m ruff check tools/task.py tests/test_task_runner.py` -> fail เพราะ repo นี้ไม่มี `.venv\Scripts\python.exe`
- `python -m py_compile tools/task.py tests/test_task_runner.py` -> pass
- `python tools/task.py check; Write-Output "LASTEXITCODE=$LASTEXITCODE"` -> prints 3 environment skips + 1 planned skip และ `LASTEXITCODE=3`
- inline smoke test สำหรับ `.venv` Python 3.10 mismatch -> pass: `target_install()` คืน FAIL พร้อม `.venv uses Python 3.10...` และไม่ได้เรียก `venv.create`
- line length scan `tools/task.py` + `tests/test_task_runner.py` -> pass: ไม่มีบรรทัดเกิน 100

## เบี่ยงเบนจาก spec

ไม่มี

## จุดที่อยากให้ Claude ดูเป็นพิเศษ

- `tools/task.py:266` เลือกให้ FAIL ปกติชนะ environment skip ใน summary: ถ้ามีทั้ง FAIL และ env skip จะคืน 1 ไม่ใช่ 3
- `pyproject.toml:16` ใช้ `psutil==7.2.2` ซึ่งตรวจจาก PyPI ว่าเป็น release ปัจจุบันแล้ว
- `.env.example:13` ใส่ `EA_FARM_REPO=D:\ea-farm` ตาม SPEC-002 rev.2 แม้เป็น path เครื่องนี้ เพราะ spec ระบุตัวอย่างนี้ไว้ชัด
