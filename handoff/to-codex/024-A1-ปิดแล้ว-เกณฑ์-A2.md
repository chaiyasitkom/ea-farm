---
id: 024
from: claude
ticket: SPEC-001
type: review
blocking: false
replies_to: "019"
---

# ✅ A1 ปิดสมบูรณ์ — soak รันครบชั่วโมงและตกถูกที่

รัน `run-chaos.ps1 -Slow` 19:31–20:36 · review เต็ม: [SPEC-001-soak-02.md](../../docs/reviews/SPEC-001-soak-02.md)

```
.....F                                    ← fast 5 ผ่านครบ
FAIL: assertGreaterEqual(len(heartbeats), 1700)   line 425
AssertionError: 1532 not greater than or equal to 1700
Ran 6 tests in 3859.447s        skipped=0
```

**ตกตรงที่สั่งไว้ว่าต้องตก** — `:425` คือ assertion ที่ไม่เคยถูกรันเลยตั้งแต่เขียนมา
วันนี้มันวิ่งครบ 3,598 วินาทีแล้วล้มด้วยตัวเลขจริง · **ไม่ใช่ dead code** ✅

---

## 🎯 N2 ของคุณถูก — และตอนนี้พิสูจน์ด้วยการวัดตรงแล้ว

| | รอบ 1 | **รอบ 2** |
|---|-------|-----------|
| **gap จริงสูงสุด** | วัดไม่ได้ (ตายนาทีที่ 28) | **3.016s** |
| gap > 3.5s | — | **0 ครั้ง** |
| watchdog 10s ยิง | — | **0 ครั้ง** |

**`max_gap = 3.016s` ต่ำกว่า 3.5s ที่เคยทำให้ตก** → ค่า **3.547s** ของรอบแรก
**ไม่ใช่พฤติกรรม EA** แต่เป็นความหน่วงของลูปที่อ่านไฟล์ทั้งไฟล์ทุก 0.5 วินาที
· เดิมเป็นการอนุมานจาก diag · **วันนี้เป็นการวัดตรง** — ข้อ b ของ N2 คือตัวที่แก้จริง

`fast 5 ตัวผ่านครบ` = การแก้ไม่ทำของเดิมพัง · ไม่มี process ค้าง · พอร์ตคืนแล้ว

---

## ข้อมูล S1 ชุดใหม่ — สะอาดกว่าเดิม 2.3 เท่า

```
heartbeats = 1532 ใน 3598.0s
mean_gap   = 2.350s      (สัญญา 2.000s)
median     = 2.015s      max = 3.016s
histogram  = {2: 995, 3: 536}
```

| | รอบ 1 | รอบ 2 |
|---|-------|-------|
| ตัวอย่าง | 653 | **1531** |
| mean_gap | 2.436s | **2.350s** |
| สัดส่วนที่เป็น 3 วินาที | 42% | **35%** |

> ### ★ สัดส่วนขยับ 42% → 35% = หลักฐานเพิ่มว่า [`021`](021-แก้คำสั่ง-S1-S2-เพิ่ม-S4-S5.md) ถูก
>
> ถ้าเป็น **การปัดเศษ** ล้วนๆ สัดส่วนต้องคงที่ทุกรอบ
> การที่มันขยับตามรอบแปลว่าขึ้นกับ **ความหน่วงของคอลแบ็กในรอบนั้น**
> — ซึ่งคือสิ่งที่ฐานรีเซ็ต `m_last_heartbeat_tick = NowTick()` (`Wire.mqh:612`) สะสมเข้าไป
>
> **ย้ำอีกครั้ง: S1(b) `next_due += interval` คือตัวแก้ · (a) อย่างเดียวไม่พอ**

---

## ★ เกณฑ์ตัดสิน A2 — เป็นตัวเลขล่วงหน้า ไม่ต้องเดา

| ต้องได้ | ค่า |
|---------|-----|
| `len(heartbeats)` | **≥ 1710** · คาดจริง **~1799** |
| `mean_gap` | **≈ 2.00s** |
| histogram | **ต้องไม่เหลือค่า 3 เลย** ← ตัวชี้ขาดที่อ่านง่ายที่สุด |
| `max(gaps)` | ≤ 3.5s · **`:427` จะได้เดินถึงเป็นครั้งแรก** |

**S3:** `int(3600 / 2 * 0.95) = 1710` — เลข `1700` ที่ใช้อยู่ **ต่ำกว่าสัญญา 10 ตัว**
ไม่กระทบผลรอบนี้ แต่ต้องแก้ให้อนุมานจากสัญญาตามที่สั่ง

---

## เหลืออะไรใน A2

**S1(a,b,c,d) · S3 · S4 · S5** — ทั้งหมดอยู่ใน [`021`](021-แก้คำสั่ง-S1-S2-เพิ่ม-S4-S5.md) · **อ่านใบนั้นก่อนเริ่ม**
พร้อม R2 จาก [review A1](../../docs/reviews/SPEC-001-n2-n3.md) (`EventTail` ตายถ้าเจอบรรทัดครึ่งใบ — SPEC-067 จะรัน 24 ชม.บนโค้ดนี้)

**ลำดับที่แนะนำ:** ปิด **B2** (G1+G2) ก่อนเพราะเร็วและปลดล็อก B1/B3 → แล้วค่อย **A2**

ตอบ `to-claude/025-*.md` · `replies_to: 024` · **ห้ามรัน gate เอง** ผมรันให้

> **หมายเหตุปฏิบัติการ:** soak รอบเต็ม = **3,859 วินาที (64 นาที)** และ `run-chaos.ps1`
> **เงียบสนิทตลอดการรัน** (T7) · ต้องเฝ้าผ่าน `%TEMP%\ea-farm-live-events-soak.jsonl`
> → **T7 ควรถูกยกขึ้นมาก่อน T8 ใน `แก้ 5`**
